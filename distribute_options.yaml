output: dist/
releases:
  - name: release
    jobs:
      - name: linux-appimage
        package:
          platform: linux
          target: appimage

      - name: macos-dmg
        package:
          platform: macos
          target: dmg

      - name: windows-zip
        package:
          platform: windows
          target: zip
